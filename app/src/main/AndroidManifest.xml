<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed only for the opt-in cloud AI layer (ADR-0002). Gated at runtime by consent +
         fails-closed logic; the app's core protection works fully offline without it. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Mic capture for live listening (opt-in). Runtime-requested; the app works via Manual Mode
         and the demo without it. Speakerphone + mic is the only compliant call-audio path (ADR-0002). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vaarta">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets us share the generated PDF via a content:// URI, required on modern Android;
             scoped to the app's own cache dir only, never exported. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
