<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed only for the opt-in cloud AI layer (ADR-0002). Gated at runtime by consent +
         fails-closed logic; the app's core protection works fully offline without it. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Mic capture for live listening (opt-in). Runtime-requested; the app works via Manual Mode
         and the demo without it. Speakerphone + mic is the only compliant call-audio path (ADR-0002). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Floating in-call copilot (Phase 4C, ADR-0003 Phase C). The overlay window draws the bubble +
         panel over the dialer; the foreground service (type=microphone) keeps live listening alive
         while the user is in the phone app. All opt-in: the overlay grant is user-driven via Settings,
         and the mic FGS only runs after RECORD_AUDIO is granted and the user taps Start. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Guardian contact picker (Task 5, spec §7). Requested at runtime only at the moment the user
         taps "Guardian contact" — needed to read back the picked contact's phone number, since the
         system contact picker's result Uri only grants access to that one Contacts row, not the
         Phone data table. Never held passively; the app has no other use for contacts. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Vaarta">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets us share the generated PDF via a content:// URI, required on modern Android;
             scoped to the app's own cache dir only, never exported. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- The live-copilot foreground service (Phase 4C). type=microphone because it owns the mic
             capture for the whole in-call session; not exported (started only by MainActivity). -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Per-app language (redesign spec §3B.1): lets AppCompatDelegate.setApplicationLocales
             persist the chosen UI language across restarts on API < 33, without needing per-locale
             activity recreation logic of our own. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>
