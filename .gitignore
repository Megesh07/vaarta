# Build outputs
build/
out/
.gradle/
.kotlin/
*.apk
*.aab

# Local machine config (SDK path is per-developer)
local.properties

# Signing — NEVER commit these (guardrail: no secrets in the repo)
*.jks
*.keystore
keystore.properties

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Android native / profiling
captures/
.cxx/
*.hprof
.navigation/

# Model packs (fetched on demand per docs/CONTRIBUTING.md §2, not checked into git)
/models/
