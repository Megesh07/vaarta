# Build outputs
build/
out/
bin/
.gradle/
.kotlin/
*.apk
*.aab

# Local machine config (SDK path is per-developer)
local.properties

# Signing / secrets — NEVER commit these (guardrail: no secrets in the repo)
*.jks
*.keystore
keystore.properties
secrets.properties
*.env

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Android native / profiling
captures/
.cxx/
*.hprof
.navigation/

# Model packs (fetched on demand per docs/CONTRIBUTING.md §2, not checked into git)
/models/

# Internal working docs (specs, ADRs, plans, process notes, task logs) — kept locally for ongoing
# development, deliberately not part of the public submission repo. README.md is the public-facing
# doc; the architecture diagram lives in /architecture/, outside this folder, so it stays tracked.
/docs/
/PROJECT_STATUS.md
/CODE_OF_CONDUCT.md
/SECURITY.md
/.superpowers/
/.claude/
